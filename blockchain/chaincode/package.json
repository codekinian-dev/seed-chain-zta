{
  "name": "seed-certification-contract",
  "version": "1.0.0",
  "description": "Chaincode Sertifikasi Benih Perkebunan",
  "main": "index.js",
  "engines": {
    "node": ">=12",
    "npm": ">=5"
  },
  "scripts": {
    "start": "fabric-chaincode-node start --peer.address=$CORE_PEER_ADDRESS --chaincode-id-name=$CHAINCODE_ID"
  },
  "dependencies": {
    "fabric-contract-api": "^2.4.0",
    "fabric-shim": "^2.4.0"
  }
}